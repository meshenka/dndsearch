import refiner from '../src/refiner';
import firost from 'firost';
console.info(firost);

// get all txt files path in order from the directory
// for each, refine them, and seav ethem


console.info(refiner);
